// Modal functionality
document.addEventListener('DOMContentLoaded', function () {
    // Get the Bootstrap modal element by ID
    var myModal = new bootstrap.Modal(document.getElementById('staticBackdrop'));

    // Show the modal
    myModal.show();
});